# Starter issue specifications

These are small, mechanically reviewable tasks that can be turned into public issues when a maintainer chooses. They are tracked specifications, not claims that an external contributor has requested or adopted the work.

Each spec contains a fixture, expected result, and acceptance test so a contributor can reproduce the task without redoing the architecture review.
