# Benchmark candidates

This directory is a staging area for proposed public benchmark cases. Files here are **not** frozen Gold and are not used by CI or model selection merely because they are committed.

Create one directory per candidate case and follow `CONTRIBUTING.md`. Promotion into a future benchmark requires independent annotation/adjudication and an explicit freeze step described in `docs/relation-annotation-guide.md`.

Do not place private reading material here.
