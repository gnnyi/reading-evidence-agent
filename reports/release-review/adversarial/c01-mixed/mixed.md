# Mixed result

A failed experiment is not always wasted time because a clear hypothesis can reveal which assumption failed. Yet without a decision rule, the same experiment may consume time without producing useful learning.
